import React from "react";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        margin: 10,
    },
    image: {
        width: '100%',
        height: 200,       
    },
    title: {
        color: 'white',
        fontSize: 32,
        fontWeight: 'bold',
        marginTop: 10      
    }
})
